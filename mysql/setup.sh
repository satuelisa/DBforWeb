# for debian-type linux (including ubuntu)
# note: you need admin rights (meaning: you need to belong to the sudoers group)                                                          
sudo apt update
sudo apt upgrade # update any packages already installed before adding new ones                                                           
sudo apt install mysql-server
sudo mysql -u root -p # the password will be blank 
